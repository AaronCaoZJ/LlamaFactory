按顺序实现以下要求：

1. 检查 @/workspace1/zhijun/LlamaFactory/scripts/eval.py 确保他是真的学会了指令遵循，而不是因为人工给了其他限制所以他只输出这些 token。

2. 修改 @/workspace1/zhijun/LlamaFactory/scripts/eval.py 使其支持自定义输入 prompt，我想要测试他的原始 VQA 能力。
