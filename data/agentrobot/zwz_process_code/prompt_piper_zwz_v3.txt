You are controlling a robot arm with two cameras:
- AgentView: first-person/front view from the robot of the workspace.
- WristView: close-up view from the gripper.

Task: {task}
Recent previous moves, newest first: {recent_moves}

Predict the next action the robot arm should execute to complete the task. Output exactly one action command from this list:
MV_FWD, MV_BACK, MV_LEFT, MV_RIGHT, MV_UP, MV_DOWN, GRASP, RELEASE, DONE


Use both views together:
- Use AgentView for broad positioning: locate the target object, destination, and end effector or held object in the workspace.
- Use WristView for local precision: judge whether the target, grasp area, or destination is centered between the gripper fingers.
- When far from the target or destination, let AgentView guide the main movement direction.
- When close to the target, grasp area, or destination, let WristView guide fine alignment and height correction.
- If one view is unclear, rely more on the other view, but do not ignore useful evidence from either view.

AgentView action meanings:
- MV_LEFT: move the end effector 2 cm toward the left side of the AgentView image.
- MV_RIGHT: move the end effector 2 cm toward the right side of the AgentView image.
- MV_FWD: move the end effector 2 cm toward the robot body, toward the near side of the AgentView image.
- MV_BACK: move the end effector 2 cm away from the robot body, toward the far side of the AgentView image.
- MV_UP: lift the gripper 2 cm away from the table (ground).
- MV_DOWN: lower the gripper up to 2 cm toward the table (ground).

WristView action meanings:
- MV_LEFT: move the gripper 2 cm toward the left side of the WristView image.
- MV_RIGHT: move the gripper 2 cm toward the right side of the WristView image.
- MV_FWD: move the gripper 2 cm toward the bottom of the WristView image, toward the robot body.
- MV_BACK: move the gripper 2 cm toward the top of the WristView image, away from the robot body.
- MV_UP: move the gripper 2 cm closer to the wrist camera, away from the table (ground).
- MV_DOWN: move the gripper up to 2 cm farther from the wrist camera, toward the table (ground).

Gripper and completion commands:
- GRASP only when both AgentView and WristView confirm that the target object or grasp area is clearly centered between the two gripper fingers.
- RELEASE only when the held object is above its destination and lowered onto it.
- DONE only when the task is already complete in the images: the object is at its destination and the gripper is clear.

Avoid repeating a direction that conflicts with the most recent move.

Before answering, remember the goal:
Task: {task}
Recent previous moves, newest first: {recent_moves}

Predict the next action the robot arm should execute to complete the task. Output exactly one action command from this list:
MV_FWD, MV_BACK, MV_LEFT, MV_RIGHT, MV_UP, MV_DOWN, GRASP, RELEASE, DONE

No punctuation. No explanation.
Your answer:
